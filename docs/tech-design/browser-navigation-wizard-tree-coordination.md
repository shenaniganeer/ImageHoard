# Browser navigation, delete/archive wizard, and folder tree coordination

Agents and contributors: follow repository-wide guidance in [`AGENTS.md`](../../AGENTS.md) (source-of-truth order, PRD traceability, and closing `ImageHoard.App` before `dotnet build` / `dotnet test`).

This note describes how **image navigation**, **delete/move (wizard) processing**, and the **browser** UI interact. The shipped browser column is **Browse2** (`BrowserV2Host`); sections below that reference the legacy mixed **`TreeView`** host remain for historical coordination context where method names still appear in traces.

## Maintenance rule

Any pull request that changes **keyboard/pointer dispatch for browse navigation**, **delete/archive wizard destructive flows**, **browser tree Find overlay**, **browser tree population, selection, or viewport scrolling** (`BrowserTreeViewportIntent`, `BrowserTreeViewportVisibility`, `BrowserTreeViewportPageScrollPlan`, `BrowserTreeViewportIntentResolver`, `BrowserTreeViewportBrowse2FolderTarget`, `BrowserPaneState`, `ListViewportScrollMath`, `MainWindow.BrowserViewport.cs` / `BrowserTreeViewportPump`, `ScheduleViewport`, `ScheduleViewportAsync`, `RunColdBootViewportAsync`, `SuppressViewportForColdBoot`, `BuildBrowserPaneState`, `PrepareBrowserTreeViewportAfterWizardMutation`, `TryBringFolderTreeNodeIntoView`, **`paths.lastActedFsObject` / `SetLastActedFsObject` / `ForColdBootAnchor`**), **folder metrics queueing or UI apply for browser folder rows** (`StartFolderMetricsWorkAsync`, `ApplyFolderMetricsSnapshotCore`, `FolderMetricsSnapshotApplyKind`, trusted subtree cache vs immediate expand state, `FolderMetricsTrust`), **tree slideshow discovery or path-store behavior** (`TreeSlideshowSession`, `SlideshowDiscoveredPathStore`, `RecursiveImageEnumerator` slideshow enumeration, `SlideshowCoordinator`, `SlideshowAlgorithmDefaults`), **path overlay list position during slideshow** (`ApplyOverlayListPositionFromTreeAsync`, `SlideshowCoordinator.TryGetSlideshowOverlayListPosition`), **preview zoom / scroll metrics (`UpdatePreviewScrollMetrics`, `ApplyFullscreenImageForFitMode`, `TryGetPreviewImageIntrinsicDips`, `RequestViewZoomActualPixelsAsync` / `TryExecuteViewZoomActualPixels`, `ReloadPreviewAfterZoomChangeAsync`, cursor-relative zoom anchors: `TryPrepareZoomAnchorFromWheelIfNeeded`, `TryApplyPendingZoomScrollAnchorForHost`, `BeginZoomCommandScrollAnchorCapture`, `FullscreenScrollViewer` / `FullscreenScrollContentGrid`)**, or **preview enqueue/drain** in this area **must update this document** in the same PR (diagrams, flags, method names, or new entry points). Prefer linking to API symbols over pasting large code blocks.

**Traceability:** This coordination surface implements product requirements **FR-BR-01** through **FR-BR-07** (browser tree, image list, sorts, favorites, folder metrics cache) and performance expectations **NFR-PF-05** (folder aggregate sorts never block the UI indefinitely) and **NFR-PF-06** (UNC/SMB paths — timeouts, cancel, feedback). When behavior changes, cite the relevant **FR-BR-*** / **NFR-PF-*** IDs in the PR.

## Browse2 (shipped browser path)

The browser column ships as **Browse2** (`ImageHoard.App.BrowserV2`, `ImageHoard.Core.Browse2`) with no host swap flag. It satisfies the same **FR-BR-*** / **NFR-PF-*** obligations as the retired mixed **`TreeView`** host, using:

- **Single writer:** `FsMap` is updated by `FsBackgroundScanner`, `FsTargetedRefresher`, and `FsChangeApplier` (wizard-driven mutations after IO succeeds). **No** constant `FileSystemWatcher`.
- **Fan-out:** `FsDiffStream` emits typed folder/aggregate events; **`TreeController`** and **`ImagePaneController`** subscribe and apply **one** batched UI update per dispatcher tick.
- **Viewport:** **`FolderTreeView`** uses **scroll anchoring** on flat model deltas (capture top realized folder row + offset → apply `FlatModelDelta` → `ChangeView` after layout) instead of `BrowserTreeViewportPump` + `LayoutUpdated` retries for **structural** stability. **Navigation-driven** folder + image list alignment uses **`MainWindow.BrowseShell.Browse2ApplyViewportIntentAsync`**: **`BrowserTreeViewportBrowse2FolderTarget`** maps **`BrowserTreeViewportIntent`** + **`BrowserPaneState`** to the folder path whose row should stay visible; **`FolderTreeView.ScrollFolderIntoView`** and **`ImagePaneView.ScrollImagePathIntoView`** share **`ListViewportScrollMath`** (`ImageHoard.Core.Browse2`): **no scroll** when the target row is already **fully** visible; otherwise pin the row top at the **third** viewport line (two leading row heights). **Find folder** hits and **cold-boot file parent** use **vertical center** for the folder row. **`CrossPaneCoordinator.ApplyFindFolderHit` / `ApplyFindFileHit`** do **not** scroll the tree (the host **`await`s `ScheduleViewportAsync(ForFindHit)`** after each hit). **`ImagePaneView`** hosts **`ScrollViewer` + `ItemsRepeater`** with a **fixed row height** (`ImageRowHeight`); scroll uses **index × row height** (same contract as the folder tree) and schedules **`DispatcherQueuePriority.Low`** restore **only after** a successful **`ChangeView`**. **`ImageScrollViewer.SizeChanged`** re-runs **`ScrollImagePathIntoView`** with **`skipIfFullyVisible: true`** so maximize/resize does not jitter. **`ImagePaneView.ScrollViewerViewChanged`** (non-intermediate) participates in debounced layout persistence like **`FolderTreeView.ScrollViewerViewChanged`**.
- **Cold boot:** Restores expansion, **selected folder**, and **persisted viewport anchor** (`AnchorFolderPath` + `OffsetWithinRowPx`); **`paths.lastActedFsObject`** stays the separate “last acted filesystem object” anchor for find/wizard/refocus semantics. See [browser-tree-viewport-anchor-persistence.md](../design-decisions/browser-tree-viewport-anchor-persistence.md).
- **Cross-cutting glue:** `CrossPaneCoordinator` replaces legacy `MainWindow.BrowserPane.cs` responsibilities (image-step → tree reveal, wizard reconciliation, slideshow overlay position, etc.). **`ImagePaneController.WaitForReloadAppliedAsync`** lets wizard / delete reconciliation await Browse2 image-list reload so **`CommitIncrementalFolderPreviewAndSelectionAsync`** reads **`Items`** after disk sync (**FR-BR-01**, **FR-BR-04**).

Architecture: [browser-tree-rewrite-architecture.md](../design-decisions/browser-tree-rewrite-architecture.md). Virtualized presenter: [browser-folder-tree-virtualization-itemsrepeater.md](../design-decisions/browser-folder-tree-virtualization-itemsrepeater.md). Legacy path→`TreeViewNode` index (superseded for Browse2): [browser-folder-tree-path-to-node-index.md](../design-decisions/browser-folder-tree-path-to-node-index.md).

**Maintenance:** Any PR that changes **`BrowserV2Host`**, **`TreeController`**, **`FolderTreeFlatModel`**, **`FsDiffStream`** contracts, **`FsChangeApplier`**, **`FolderTreeView`** anchoring or **viewport scroll** (`ScrollFolderIntoView`, `ListViewportScrollMath`), **`ImagePaneController`**, **`ImagePaneView`** (repeater, **`ScrollImagePathIntoView`**, selection chrome), **`CrossPaneCoordinator`**, **`MainWindow.Browse2Session`** (coordinator lifetime / refresh wiring), or **browser tree settings** for Browse2 must update this section **and** the linked ADRs when user-visible coordination changes.

### Browse2 targeted refresh triggers (`FsTargetedRefresher.RefreshAsync`)

| Trigger | Location / notes |
|---------|-------------------|
| Cold boot | `CrossPaneCoordinator.ColdBoot` → `RefreshAsync(IndexRoot)` after rebuild; starts **`FsBackgroundScanner`** after first frame. |
| Re-navigate same index root | `Browse2EnsureCoordinatorForCurrentBrowseAsync` reuse branch → `Browse2RefreshVisibleFoldersAsync`. |
| Window activation | **Does not** call `Browse2RefreshVisibleFoldersAsync` (avoids spurious image-pane reload / preview blank on focus return). After external disk edits while the app was in the background, use **`browse2.refreshTree` (F5)**, navigation, expand, find, or wizard-driven reconciliation. |
| User expand in folder tree | `CrossPaneCoordinator.OnFolderTreeToggleExpand`: after expand, when map row is unverified or `HasSubfolders` with zero child rows in map. |
| Find folder / file hit | `CrossPaneCoordinator.ApplyFindFolderHit` / `ApplyFindFileHit` → refresh hit folder or parent of file; folder-tree scroll is **`Browse2ApplyViewportIntentAsync`** after **`ScheduleViewportAsync(ForFindHit)`** (no duplicate `ScrollFolderIntoView` in the coordinator). |
| Explicit command | `browse2.refreshTree` (KeyboardOnly default **F5**) → `Browse2RefreshVisibleFoldersAsync`. |
| Wizard / app IO | `FsChangeApplier` (existing); no live `FileSystemWatcher`. |

### Legacy vs V2 reference map

| Legacy (`TreeView` host) | Browser V2 |
|--------------------------|------------|
| `ScheduleViewport` / `ScheduleViewportAsync` / `BrowserTreeViewportPump` | Scroll anchoring + targeted `ChangeView` for explicit reveal |
| `_populateBrowserGeneration` | Sequential `FsMap` + UI batching (no generation token for superseded tree append on the V2 path) |
| `EnterBrowserPaneMutation` / `IsBrowserPaneMutationInProgress` blocking `nav.*` | Wizard patches `FsMap` via `FsChangeApplier`; UI applies coalesced diffs with anchoring; input policy may still gate during modal wizard (unchanged product rule) |
| Cold boot: `RunColdBootViewportAsync` + `ForColdBootAnchor` + `lastActedFsObject` | Cold boot: viewport **anchor DTO** + selection + expansion + `FsMap` build; `lastActedFsObject` still updated for action semantics |
| `_folderTreeNodeByPath` hot index | `FolderTreeFlatModel` path→row / index (flat projection) |

## Input routing

| Path | Location | Notes |
|------|----------|-------|
| Text input vs profile chords | `MainWindow.TryDispatchInputCommand` (`MainWindow.xaml.cs`) | When focus is inside a standard text control (`IsInsideTextInput`), returns without matching merged keyboard bindings so **PreviewKeyDown** tunneling does not mark keys handled before the editor consumes them (inline rename in the browser `TreeView`, preferences fields, etc.). |
| Browser tree focus (arrows, expand/collapse, delete) | `FolderTree_PreviewKeyDown` → `TryDispatchBrowserTreeInputCommand` before `TryDispatchInputCommand` (`MainWindow.xaml.cs`, `MainWindow.BrowserPane.cs`) | Chords for `browse.treeNext`, `browse.treePrevious`, `browse.treeExpand`, `browse.treeCollapse`, and **`browse.treeDelete`** are compiled into **`BrowserTreeKeyboardDispatchTable`** from the merged profile. Those command rows are **excluded** from the main `KeyboardDispatchTable` so the same physical keys still bind to `nav.nextImage` / `nav.prevImage` when focus is outside the tree (for the arrow keys). When focus is inside `FolderTree` (`IsFocusInsideBrowserTree`) and rename/text-input defer is false, the tree table is matched first. **`browse.treeNext` / `browse.treePrevious`** (`BrowseTreeKeyboardMoveSelection`): if the selected row is an **`ImageRow`**, they delegate to **`BrowseNavigateByStep`** (same image stepping as global `nav.nextImage` / `nav.prevImage`, including browse navigation mode). If the selected row is a **`FolderTreeEntry`**, they call **`BrowseNavigateSiblingFolderFromInput`** with a **`contextDirectoryOverride`** set to that folder path so sibling order follows **`FolderDirectorySort`** / **`_layoutState.FolderListSort`** without stale preview paths winning in **`BrowseContextDirectory.Resolve`**. Otherwise (no folder/image row), selection still moves by the visible preorder list **`EnumerateVisibleBrowserTreeNavRows`**. **`browse.treeDelete`** is executed only with tree focus (`TryExecuteInputCommand`) and calls **`QueueExecuteBrowserTreeDeleteFromKeyboardAsync`** → **`ExecuteBrowserTreeDeleteForCurrentSelectionAsync`** (`MainWindow.BrowserTreeDelete.cs`). When **`browse.treeCollapse`** hides the previewed file, **`FolderTree_OnCollapsed`** clears the preview via **`ClearImageSelectionAndPreviewCore`** and passes the **collapsed folder `TreeViewNode`** so selection returns to that folder row and **`FolderTree.Focus`** keeps keyboard routing in the tree. `InputBindingConflictChecker` exempts only **pairwise** keyboard fingerprint collisions between `nav.nextImage`/`nav.prevImage` and the four **arrow/expand/collapse** `browse.tree*` commands (three or more distinct commands on one chord still warn). |
| Root-only browse/sort keys | `MainWindow.RootGrid_KeyDown` → `ShouldDeferAppKeyboardShortcuts` (`MainWindow.xaml.cs`) | Defers F2 rename, Enter→fullscreen from the tree, and `HandleSortKeyboardShortcuts` (K/D/U) while `IsInsideTextInput` **or** `_renameTargetNode != null` (covers the dispatcher tick before rename `TextBox` focus lands). In fullscreen, `RootGrid_KeyDown` returns early so it does **not** call `HandleSortKeyboardShortcuts`; merged-profile chords still dispatch from `RootGrid_PreviewKeyDown` and `FullscreenLayout_KeyDown`, and **`FullscreenLayout_KeyDown`** also invokes `HandleSortKeyboardShortcuts` when the chord table did not handle the key (same defer rules). |
| Command execution | `MainWindow.TryExecuteInputCommand` (`MainWindow.xaml.cs`) | Central switch on `commandId`. |
| Wizard overlay open | Same file | When `IsDeleteArchiveWizardOverlayOpen`, only a whitelist of commands runs; others return without acting. |
| Find-in-tree overlay open | Same file | When `IsBrowserFindOverlayOpen`, only `ui.escape` and `browse.findInTree` run; other `commandId` values return without acting. |
| Preferences overlay open | Same file | Whitelist includes `ui.escape`, `settings.open`, and `browse.findInTree` (opening Find hides Preferences first). |
| Browser pane mutation | `_browserPaneMutationDepth` / `IsBrowserPaneMutationInProgress` (`MainWindow.xaml.cs`) | Non-zero while the tree and preview context are being reconciled after wizard deletes/moves (and related paths). Browse-style `nav.*` commands, slideshow commands **`slideshow.start`**, **`slideshow.switchToBrowseAtCurrentLocation`**, **`slideshow.siblingNextImage`**, **`slideshow.siblingPrevImage`**, **`slideshow.deleteCurrent`**, and the tree-scoped `browse.treeNext` / `browse.treePrevious` / `browse.treeExpand` / `browse.treeCollapse` / **`browse.treeDelete`** commands return **handled** without acting so input does not interleave with tree updates. |
| Content dialog from wizard | `_contentDialogModalDepth` (`MainWindow.xaml.cs`) | Used to suppress spurious tree collapse during modal focus churn (`FolderTree_OnCollapsed` in `MainWindow.BrowserPane.cs`). |
| Wheel / pointer chords | `TryDispatchPointerWheelBindings`, pointer handlers (`MainWindow.xaml.cs`) | Eventually call `TryExecuteInputCommand`. Wheel zoom on **`PreviewScrollContentGrid`** / **`FullscreenScrollContentGrid`** uses tunneling handlers so **`TryPrepareZoomAnchorFromWheelIfNeeded`** can run before the **`ScrollViewer`** consumes the wheel; **`RootGrid_PointerWheelChanged`** ignores wheel when the original source is under those content grids to avoid double dispatch. |

## Browser tree selection and in-tree delete

- **`FolderTree`** uses default **single** `TreeView` selection (no `SelectionMode="Multiple"`). Pointer selection is handled by the control; **`FolderTree_SelectionChanged`** drives preview via **`ApplyBrowserTreeLeadPreviewAsync`** (image rows → **`OnFolderTreeImageRowSelectedAsync`**; otherwise **`ClearBrowserPaneImagePreviewStatePreserveTreeSelection`** when a preview was showing).
- **Context menu Delete** (`BrowserContextDelete_Click`, `MainWindow.BrowseShell.cs`): syncs selection to **`_browserContextMenuTargetNode`** when present, then **`ExecuteBrowserTreeDeleteForCurrentSelectionAsync`** (Browse2: at most one nav row via **`GetSelectedBrowserTreeNavNodes`**). **`WizardExecuteImageRecycleOrPermanentBatchAsync`** ends with **`RefreshBrowserPaneAfterWizardImageDeletesAsync`**, which refocuses the image pane; the tree-delete path does **not** clear selection afterward (no trailing blanket clear of **`Images.SelectedImagePath`**).
- **Recycle Bin** deletes use **`WizardExecuteImageRecycleOrPermanentBatchAsync`** (files; **`ShellBulkRecycle`** / **`IFileOperation`** batch to Recycle Bin with sequential **`ShellRecycle`** fallback, then bounded-parallel **`File.Delete`** for user-approved permanent deletes) and **`ExecuteSendFolderToRecycleBinAfterConfirmAsync`** (folders) after **`ContentDialog`** confirmation; selected paths are filtered and ordered with **`BrowserTreeDeletePathDedupe`** (`ImageHoard.Core.Browse`).
- **Open archive wizard in this folder** (`BrowserContextOpenArchiveWizardInFolder_Click`, `_browserTreeContextMenu` in **`WireBrowserTreeTemplates`**, `MainWindow.BrowserPane.cs`): resolves the folder from the browse toolbar’s current folder or the context row (**`FolderTreeEntry.Path`** or **`ImageRow`**’s parent directory), **`SyncBrowseTreeSelection`** for tree rows, then **`ShowOrActivateDeleteArchiveWizardForFolder`**. The path must pass **`TreeDeletePathsAreUnderBrowseRoot`** (same containment rule as tree delete). The wizard stores **`_deleteArchiveWizardFolderPathOverride`** so **`TryGetDeleteArchiveWizardWorkingFolder`** returns that directory **before** **`TryResolveDeleteArchiveWizardDirectoryFromCurrentImage`**, enabling empty folders without a current image; the override is cleared in **`NotifyDeleteArchiveWizardClosed`** and at the start of **`ShowOrActivateDeleteArchiveWizard`** (keyboard **`sort.deleteArchiveWizard`**). Browse-root relocate (`MainWindow.BrowserPane.cs`) updates the override with **`RelocateFilesystemPathUnderRoot`** alongside **`_deleteArchiveWizardCapturedWorkingFolder`**.

While the delete/archive wizard is visible, **Escape** and **Close** are limited when `IsBrowserPaneMutationInProgress` is true: the overlay root handler and `TryDismissTopModalForEscape` consume Escape without dismissing; Close is disabled (`DeleteArchiveWizardPanel.SetBrowserPaneMutationBlocking`).

## Find in tree overlay

| Concern | Location | Notes |
|---------|----------|-------|
| Command | `browse.findInTree` | Default chord **Control+F** in `defaults/input-profiles/keyboard-only.v1.json`; rebindable in Preferences (see `CommandCatalog`). |
| Chrome | Browse → Find in tree… | `MainWindow.xaml`; handler `BrowseFindInTree_Click` / `ShowBrowserFindOverlay` (`MainWindow.BrowserFind.cs`). |
| Mutual exclusion | `ShowBrowserFindOverlay`, `ShowOrActivatePreferences`, `ShowOrActivateDeleteArchiveWizard`, **`ShowOrActivateDeleteArchiveWizardForFolder`** | Opening Find hides Preferences and the delete/archive wizard; opening Preferences or the wizard (either entry point) hides Find. |
| Escape stack | `TryDismissTopModalForEscape` (`MainWindow.ModalOverlays.cs`) | Order: **Find** → Preferences → delete/archive wizard. |
| Host + panel | `BrowserFindPanel`, `RunBrowserFindSearchFromPanelAsync`, `BrowserFindStepMatchAsync`, `BrowserFindApplyMatchAsync`, `InvalidateBrowserFindCachedResults` (`MainWindow.BrowserFind.cs`) | Shallow mode uses `EnumerateVisibleFolderTreeNodesPreorder` / `CollectVisibleImageNodesPreorder` (`MainWindow.BrowserPane.cs`); deep mode enumerates the filesystem under `_currentFolderPath` on a thread-pool task with cancellation. Overlay status shows match index only (`x of y`, no target name). Changing match mode, search-in target, or deep search (via Options flyout) clears cached matches, cancels an in-flight deep search, and resets status so **Next**/**Previous**/**Enter** run a fresh search. **Folder** matches: `TryEnsureFolderPathMaterializedAsync` with `expandAndPopulateDestinationFolder: false` (ancestors expand so the hit folder row exists under expanded parents; hit folder stays collapsed), `SyncBrowseTreeSelection` on that folder row, then **`await ScheduleViewportAsync(BrowserTreeViewportIntentResolver.ForFindHit(BuildBrowserPaneState(), match))`** — the folder-tree viewport pump brings the row into view with `LayoutUpdated`-bounded retries (see **Browser tree viewport** below). **`SetLastActedFsObject(match.Path)`** records the find hit for cold-boot anchor. **File** matches: `EnqueuePreviewNavigation`; **`TrySyncBrowseTreeSelectionToImagePathAsync`** expands/materializes the parent chain so the **image row** exists and is selected; then the same **`ScheduleViewportAsync` + `ForFindHit`** path (intent uses **`PreferSelectionFirst: true`**, image path as **`PrimaryPath`**, parent directory as **`SecondaryPath`** when under browse root). **`SetLastActedFsObject`** on the file path. |
| Keyboard vs query box | `BrowserFindPanel` + `TryDispatchInputCommand` | Global merged chords are not dispatched while focus is inside a `TextBox` (`IsInsideTextInput`). The query box handles **Enter** (run search), **Right** at end of text and **Left** at start (next/previous match when matches exist). The overlay root handles **Left**/**Right** when focus is not in a text field so arrows still advance matches from buttons and options. **`IsDefault` on Search** is not used in XAML (WinUI markup compiler rejects `IsDefault` on this `UserControl` button); Enter still runs search from the query field. |

## Navigation pipeline (non-slideshow)

1. **`BrowseNavigateByStep`** (`MainWindow.BrowserPane.cs`) increments the nav counter, resets **`MouseButtonClickChainTracker`** (`_mouseButtonClickChainTracker.Reset()`), and starts **`BrowseNavigateByStepAsync`** without awaiting (fire-and-forget). **`OnPreviewImagePathCommitted`** (`MainWindow.PreviewScroll.cs`) also resets that tracker whenever the committed preview path changes or is cleared so a click after next/prev/first/last image (or slideshow advance, tree-driven preview, etc.) is **click 1**, not a spurious second click in the Win32 / **`PreviewImagePaneMultiClickThresholdMs`** window for chords such as **`Left` + `clickCount: 2`** bound to **`nav.lastImage`**.
2. **`BrowseNavigateByStepAsync`** resolves browse context from `_browseNavAnchorPath`, tree selection, `_currentImageFullPath`, `_currentFolderPath`, loads paths, computes index, then **`EnqueuePreviewNavigation`**, **`SetLastActedFsObject(nextPath)`**, and tree sync (**`SyncBrowseTreeSelection`** / **`TrySyncBrowseTreeSelectionToImagePathAsync`**), then **`ScheduleViewport(BrowserTreeViewportIntentResolver.ForImageStep(...))`**. The resulting intent uses **`BrowserTreeViewportVisibility.PageWhenOutsideViewport`**: the tree **`ScrollViewer`** does **not** re-pin the selected image row to the top on every step; if the row is fully visible, the viewport stays put, and if the row leaves the viewport the pump scrolls by **whole viewport heights** until the row fits (virtualized rows still rely on the existing **`LayoutUpdated`** retry loop until a **`TreeViewItem`** container exists). Other viewport reasons keep **`BringIntoView`** alignment behavior.
3. **`BrowseNavigateSiblingFolderFromInput`** → **`BrowseNavigateSiblingFolderAsync`** for `nav.nextDirectory` / `nav.prevDirectory` (and for **`browse.treeNext` / `browse.treePrevious`** when a folder row is selected). Optional **`contextDirectoryOverride`** skips **`BrowseContextDirectory.Resolve`** after validating the path is under the browse root (**`BrowseContextImageSequence.IsContextDirectoryUnderBrowseRoot`**). Uses **`_populateBrowserGeneration`** to drop stale results after repopulation; wizard **`RefreshBrowserPaneAfterWizardImageDeletesAsync` / `RefreshBrowserPaneAfterWizardUndoAsync`** and **`ReconcileBrowserPaneAfterWizardNavigateToParentAsync`** (via **`NavigateToFolderAsync`**) also advance this generation so staged folder population cannot interleave after tree reconciliation. When navigating among siblings under the same browse root (non-root context), after collapsing the prior folder the code **`await ScheduleViewportAsync(ForSiblingFolderNav(...))`** once so layout can settle inside the pump (no separate pre-collapse viewport pass). **`SetLastActedFsObject(targetFolderPath)`** on success.

When `IsBrowserPaneMutationInProgress` is true, **`BrowseNavigateByStep`** returns immediately (no async work); **`BrowseNavigateByStepAsync`** and **`BrowseNavigateSiblingFolderAsync`** no-op at entry.

## Preview drain

`MainWindow.PreviewNavigation.cs`: **`EnqueuePreviewNavigation`**, **`DrainPreviewNavigationCoreAsync`**, coalescing, **`SyncTreeSelectionToImagePath`**. **`SyncTreeSelectionToImagePath`** is used for **browse** re-anchor after a coalesced drain (`!_slideshowUiActive && didCoalesce`) and for other browse paths that call **`TrySyncBrowseTreeSelectionToImagePathAsync`** (Find, cold boot, keyboard tree moves, **`SwitchToBrowseAtCurrentSlideshowLocationAsync`**). **Tree slideshow** advances (`_slideshowUiActive`) must **not** call **`SyncTreeSelectionToImagePath`** from **`DecodeAndCommitPreviewAsync`** — that would rebind **`CrossPaneCoordinator.Images.CurrentFolderPath`** to each slide’s parent folder and reload the image pane (unintended “navigated into that folder” UX; legacy **`TreeView`** did not change **`_currentFolderPath`** per slide). Slideshow tree stepping is still only through merged input chords (`TryExecuteInputCommand` → `TryMoveNextTree` / `TryMovePreviousTree`); when `IsBrowserPaneMutationInProgress` is true, those commands return handled without acting.

### Tree-scope slideshow overlay (list position)

When **`_slideshowUiActive`** and the overlay is in **tree** mode, the path overlay (`ApplyOverlayListPositionFromTreeAsync` in `MainWindow.BrowseShell.cs`) uses **`SlideshowCoordinator.TryGetSlideshowOverlayListPosition`** → **`TreeSlideshowSession.TryGetTreeOverlayPosition`**: text is **`N/M · discovered D`** where **`N`** is 1-based position in the **linear display history** (including redo), **`M`** is **`history.Count`**, and **`D`** is **`DiscoveredImageCount`** (paths appended by the background enumerator for this session). **Sibling** slideshow overlay continues to show ordered sibling **`index/total`** for that directory. See [`docs/design-decisions/slideshow-algorithm-p0.md`](../design-decisions/slideshow-algorithm-p0.md) for shuffle, uniform sampling, and RAM + spill storage.

**Preview user zoom** (`MainWindow.PreviewScroll.cs`): **`_previewUserZoomFactor`** scales the laid-out preview (and fullscreen) relative to the **Image fit** baseline computed from **EXIF-oriented file pixel dimensions** (`TryGetPreviewImageIntrinsicDips`; **1:1** fit mode still uses decoded bitmap dimensions because the decode may be max-edge capped). **`WicBitmapLoader.DecodeWithOrientationAsync`** returns a **`WicBitmapDecodeResult`** so each commit caches **`RememberPreviewOrientedPixelSize`** without a second file read. **`OnPreviewImagePathCommitted`** resets zoom to **1** when the **committed preview file path** changes (and clears pending zoom-scroll anchors); it also resets **`MouseButtonClickChainTracker`** on path change or clear so multi-click mouse chords do not carry across image navigation. **`ClearImageSelectionAndPreviewCore`** clears the anchor; **`NavigateToFolderAsync`** resets zoom on folder navigation; **`TryExecuteInputCommand`** handles **`view.zoomIn`**, **`view.zoomOut`**, **`view.zoomResetFit`**, and **`view.zoomActualPixels`** via **`TryRequestViewZoomActualPixelsFromInput`** → **`RequestViewZoomActualPixelsAsync`** → **`ViewZoomActualPixelsCoreAsync`** (Ctrl+wheel zoom is gated to the preview host / fullscreen scroll content via **`IsPointerChordAllowedForCommand`** for in/out only). **`view.zoomIn` / `view.zoomOut` / `view.zoomResetFit`** bump **`_previewInvalidateGeneration`** and **`ReloadPreviewAfterZoomChangeAsync`** (`MainWindow.PreviewNavigation.cs`) so **`CreateWicDecodeLayout`** (`MainWindow.DecodeTargets.cs`) can enlarge the WIC **Fit** target box by **`_previewUserZoomFactor`** and re-decode detail for the same path instead of compositor-upscaling a viewport-sized decode. **`view.zoomActualPixels`**: when **Shrink only** / **Shrink and stretch** have fit-decoded below the capped full-resolution size, **`WicBitmapLoader.GetOrientedPixelDimensionsAsync`** compares oriented file size to the current decode; if a larger WIC output is needed, **`SetForceNextPreviewFullResolutionDecode`** makes the next **`CreateWicDecodeLayout`** use **`BitmapUniformKind.FullResolution`** (same max-edge cap as **1:1** fit), then **`CommitPreviewImmediatelyAsync`** re-decodes; **`TryExecuteViewZoomActualPixels`** applies 1:1 layout using oriented intrinsics. **`RememberDecodeTargetBox`** records the **zoom-inclusive** physical Fit target after commits (and the viewport-only box after a full-resolution commit) so **`ReloadIfDecodeTargetBoxChangedAsync`** debounce compares consistently. Resize-only **`UpdatePreviewScrollMetrics`** on the same image does not reset zoom.

**Image fit modes** (`MainWindow.P0.cs` **`ImageFitMode`**, **`view.cycleFitMode`**): P0 exposes three radio modes under **Image → Image fit** plus **`view.zoomResetFit`**. **`Shrink only`** and **`Shrink & stretch`** both decode with **`BitmapUniformKind.Fit`** against the preview/fullscreen target box multiplied by **`_previewUserZoomFactor`** (`CreateWicDecodeLayout` in `MainWindow.DecodeTargets.cs`). Display baseline (`ComputeImageDisplayBaselineDipsWindowed` / `ComputeImageDisplayBaselineDipsFullscreen` in `MainWindow.PreviewScroll.cs`): **`Shrink & stretch`** uses uniform **contain** scale via **`PreviewCoverLayout.UniformContainScale`** (entire image inside the viewport, including compositor upscale when the laid-out intrinsic is smaller than the viewport). **`Shrink only`** uses **`PreviewCoverLayout.UniformContainScaleShrinkOnly`** (same contain rule capped at oriented intrinsic logical size, so no upscaling at zoom 1). **`1:1`** uses full-resolution decode and decoded DIP baseline. **`ApplyFullscreenImageForFitMode`** applies the same baseline × zoom sizing for the fullscreen `Image` inside **`FullscreenScrollContentGrid`** (hosted by **`FullscreenScrollViewer`**, mirroring the windowed preview scroll host).

**Cursor-relative zoom (preview + fullscreen)** (`MainWindow.PreviewScroll.cs`, `MainWindow.xaml.cs`): Before **`_previewUserZoomFactor`** changes, the app records a pending anchor (viewport point in DIP relative to the active **`ScrollViewer`**, plus normalized **(u,v)** on the displayed image rect) from the **mouse wheel** when **`TryPrepareZoomAnchorFromWheelIfNeeded`** runs ahead of **`TryExecuteInputCommand`** for **`view.zoomIn`**, **`view.zoomOut`**, or **`view.zoomResetFit`** (chord allowed only over **`PreviewHostGrid`** or **`FullscreenScrollContentGrid`**). Keyboard/toolbar zoom calls **`BeginZoomCommandScrollAnchorCapture`**, which anchors to the **viewport center** unless the wheel path set **`_suppressNextZoomAnchorCenterCapture`**. After layout, **`TryApplyPendingZoomScrollAnchorForHost`** adjusts **`ScrollViewer.ChangeView`** so the same image pixel stays under that viewport point (clamped to scroll extents). **`PrepareFullscreenEnterNavigation`** / **`PrepareFullscreenExitNavigation`** reset scroll and clear pending anchors around fullscreen toggles. **`view.panPreview`** drags update whichever **`ScrollViewer`** is active (preview vs fullscreen).

### Sort-flag target path vs preview (`TryGetSortFlagTargetPath`)

`MainWindow.BrowseShell.cs`: **`TryGetSortFlagTargetPath`** chooses the file that **Keep / Delete / Unset** commands (`sort.flag*` and legacy K/D/U) affect. The info overlay’s flag glyph is driven by **`_currentImageFullPath`**, so if the Browse2 image list selection lags the committed preview (e.g. tree slideshow does not move the image pane selection on each slide), commands must still target the **preview** path or the UI looks broken in fullscreen.

Resolution order:

1. While **`_slideshowUiActive`** and the preview path is non-empty → use **`_currentImageFullPath`**.
2. Else if the tree has an **`ImageRow`** selected and it **differs** from **`_currentImageFullPath`** (both set) → use **`_currentImageFullPath`** (preview wins over stale selection).
3. Else if an **`ImageRow`** is selected → use that row’s path (including “selection with no preview” cases).
4. Else → **`_currentImageFullPath`** when set.

**Browse2 + tree slideshow:** After a successful bitmap commit in **`DecodeAndCommitPreviewAsync`**, the app **does not** call **`SyncTreeSelectionToImagePath`** while **`_slideshowUiActive`**. The folder tree, **`ImagePaneController.CurrentFolderPath`**, and image-pane selection therefore stay where the user left them during browse (parallel to legacy per-slide behavior where **`_currentFolderPath`** was unchanged). The committed slide is shown only in **preview / fullscreen**; **`_currentImageFullPath`** drives overlays and sort-flag commands. To open the slide’s folder in Browse2, the user uses **`slideshow.switchToBrowseAtCurrentLocation`** (**`SwitchToBrowseAtCurrentSlideshowLocationAsync`**: **`NavigateToFolderAsync(parent)`** + **`TrySyncBrowseTreeSelectionToImagePathAsync`**).

## Destructive / reconciliation pipeline (wizard + browser)

| Step | API | Role |
|------|-----|------|
| User confirms delete/move | `WizardExecute*` in `MainWindow.DeleteArchiveWizard.cs` | File system and navigation side effects. **Move to archive** builds a case-insensitive union of (optional) inverse-keep paths and all **Delete**-flagged image paths in the folder, then runs **`WizardExecuteImageRecycleOrPermanentBatchAsync`** (parallel pre-scan; shell batch recycle via **`ShellBulkRecycle`** with **`ShellRecycle`** fallback; user-approved permanent deletes use bounded parallelism) before `MergeMoveDirectoryAsync`. When that batch is immediately followed by a directory merge, the batch may pass **`deferBrowserPaneRefresh: true`** so the UI reconciles once after the merge succeeds (or once on merge failure using captured delete stats). |
| Reconcile tree + preview | `RefreshBrowserPaneAfterWizardImageDeletesAsync`, `RefreshBrowserPaneAfterWizardUndoAsync` (`MainWindow.BrowseShell.cs`) | Syncs Browse2 map/listings with disk, updates aggregates, refocuses selection. Merges optional **`BrowserTreeRefocusAfterWizardContext`** with delete stats: **`DeletedImagePathsForRefocus`** and **`ImagePanePathsBeforeDeletion`** (captured from **`ImagePaneController.Items`** before **`Browse2NotifyWizardImageDeletesAsync`**; deferred batch also snapshots order before recycle when **`deferBrowserPaneRefresh: true`**). Wrapped with **`EnterBrowserPaneMutation` / `LeaveBrowserPaneMutation`** (try/finally). Each method **`Interlocked.Increment`s `_populateBrowserGeneration`** at the start of the refresh so in-flight **`AppendBrowserStagedToTargetAsync`** work from the prior browse cannot resume with a stale token after disk sync. |
| Wizard parent navigation (single bump + viewport) | **`ReconcileBrowserPaneAfterWizardNavigateToParentAsync`** (`MainWindow.BrowseShell.cs`) | After **folder recycle** or **Move to archive** when the removed folder was the browsed folder: calls **`NavigateToFolderAsync(preferredNextSibling ?? parent, suppressViewportAfterRootPopulate: true)`** when **`TryComputePreferredNextSiblingFolderAsync`** returned a folder that still exists (else parent), then **`ClearImageSelectionAndPreviewCore`**, **`CommitIncrementalFolderPreviewAndSelectionAsync`**, and **one** awaited **`ScheduleViewportAsync(BrowserTreeViewportIntentResolver.ForWizardCommit(..., BrowserTreeViewportReason.AfterWizardNavigateToParent, ...))`**. |
| Image list reload gate | **`ImagePaneController.WaitForReloadAppliedAsync`** (`BrowserV2/ImagePaneController.cs`) | Completes after the dispatcher applies **`Items`** for the current reload generation (coalesced **`RequestReload`**). Used by **`CommitIncrementalFolderPreviewAndSelectionAsync`** so first-image / post-delete selection reads the post-mutation list. |
| Adjacent image after deletes (Core) | **`BrowseContextImageSequence.PickNextDisplayedPathAfterRemovalsInOrderedList`** (`ImageHoard.Core.Browse`) | Pure helper: given browse-ordered paths **before** deletion and the removed set, returns the next remaining path after the last removed index, else the previous remaining path (aligned with sibling-folder **`PickAdjacentSiblingAfterRemoval`** semantics). Unit-tested in **`BrowseContextImageSequencePickNextTests`**. |
| Defer batch refresh for chained IO | **`WizardExecuteImageRecycleOrPermanentBatchAsync(..., deferBrowserPaneRefresh: true)`** (`MainWindow.DeleteArchiveWizard.cs`) | Skips end-of-batch **`RefreshBrowserPaneAfterWizardImageDeletesAsync`**; stores succeeded delete stats and refocus context in **`_deferredWizardBatchSucceededStats`** / **`_deferredWizardBatchRefocusContext`** (deferred context includes **`ImagePanePathsBeforeDeletion`** captured from **`ImagePaneController.Items`** immediately before recycle). **`ClearDeferredWizardBatchBrowserRefreshCapture`** runs after a successful **`MergeMoveDirectoryAsync`**; on merge failure, **`RefreshBrowserPaneAfterWizardImageDeletesAsync`** runs once with the deferred payload. |
| Suppress viewport on root populate | **`NavigateToFolderAsync(path, suppressViewportAfterRootPopulate)`**, **`PopulateBrowserRootsCoreAsync`**, **`FinalizeBrowserRootPopulateAfterImmediateAppend`**, **`AppendBrowserStagedToTargetAsync`** (`MainWindow.BrowserPane.cs`) | When true, root browse populate skips **`ScheduleViewport` / `ScheduleViewportAsync`** at populate completion so a caller (e.g. **`ReconcileBrowserPaneAfterWizardNavigateToParentAsync`**) performs a single scroll pass after selection commit. |
| Cold boot session restore | **`RestoreStartupBrowseAsync`** (`MainWindow.xaml.cs`), **`NavigateToFolderAsync(..., coldBootSessionRestore: true)`**, **`PopulateBrowserRootsCoreAsync(..., deferRootSelectionForColdBootRestore)`**, **`FinalizeBrowserRootPopulateAfterImmediateAppend`** / **`FinalizeBrowserRootPopulateSelectionAndChrome`**, **`RestoreColdBootBrowseAfterNavigateAsync`** (`MainWindow.BrowseShell.cs`), **`SuppressViewportForColdBoot`** / **`_suppressBrowserTreeViewportMutationForColdBoot`**, **`RunColdBootViewportAsync`**, **`BrowserTreeViewportIntentResolver.ForColdBootAnchor`**, **`paths.lastActedFsObject`** (`AppSettingsStore` / `AppSessionSettings`) | See **Cold boot and tree scroll persistence** below. Summary: **`RestoreStartupBrowseAsync`** enables suppression for the whole **`NavigateToFolderAsync` + `RestoreColdBootBrowseAfterNavigateAsync`** block so incidental **`ScheduleViewportAsync`** / **`ScheduleViewport`** are no-ops while expansion and selection run (**Browse2:** **`Browse2ApplyViewportIntentAsync`** still runs **`ColdBootRestore`**). After restore, **`RestoreColdBootBrowseAfterNavigateAsync`** awaits **`RunColdBootViewportAsync(ForColdBootAnchor(...))`** so the last acted-on filesystem object (**`LastActedFsObject`**) is visible (**Browse2:** directory anchor → third-row pin when scrolling (skip if visible); file anchor → parent folder row vertically centered (skip if visible); missing/stale anchor → scroll list to top; **legacy `TreeView` host:** folder → top of viewport, expanded; file → vertically centered in tree; missing/stale anchor → scroll list to top). **`NavigateToFolderAsync`** clears **`LastActedFsObject`** when the browse root path changes (not cold boot). **`NavigateToFolderAsync`** normalizes the browse path with **`Path.GetFullPath`** after **`Directory.Exists`** for consistent snapshot root matching. |
| Disk ↔ tree image merge | `SynchronizeFolderImageRowsWithDiskAsync` (`MainWindow.BrowserPane.cs`) | Dedupes sorted disk image rows by full path (case-insensitive) before merge; detects duplicate image paths or duplicate `TreeViewNode` references among sibling image rows (and duplicate refs inside the merged disk-order list) to skip the structural no-op fast-path and rebuild; merge always appends one `TreeViewNode` per deduped row. After wizard **`Refresh*`**, the browsed-folder sync uses **`forceStructuralImageRebuild: true`** via **`SynchronizeCurrentFolderImageNodesWithDiskAsync`** so the **`identicalStructure`** no-op path is skipped for **`FolderTree.RootNodes`**. |
| Tree expand (user) | `FolderTree_OnExpanding` (`MainWindow.BrowserPane.cs`) | When the user expands a folder row: if children were already materialized, **`SynchronizeFolderImageRowsWithDiskAsync`** re-lists that folder’s images vs disk (unless **`IsBrowserPaneMutationInProgress`** or inline rename **`_renameTargetNode`** is set); **`EnqueueFolderMetricsScanIfNeeded`** runs for the folder subtree. First-time expand still uses **`PopulateFolderTreeNodeChildrenAsync`**. |
| Browse nav from tree | `GetBrowseNavigationPathsForContextAsync`, `DedupePathsPreserveOrder`, `CollectImageNodesForBrowseContextDirectory` (`MainWindow.BrowserPane.cs`) | Tree-backed path lists dedupe case-insensitively with first occurrence kept so overlay `1/N` and sequential nav match distinct files; image node collection uses first materialized row per path. |
| Wizard refresh sibling sync | `RefreshBrowserPaneAfterWizardImageDeletesAsync`, `RefreshBrowserPaneAfterWizardUndoAsync` (`MainWindow.BrowserPane.cs`) | Tracks which sibling `IList<TreeViewNode>` instances were already reconciled (`RootNodes` plus expanded subfolders) and skips a second `SynchronizeFolderImageRowsWithDiskAsync` on the same list reference in one refresh pass. |
| Pick next image or folder row | `CommitIncrementalFolderPreviewAndSelectionAsync` (`MainWindow.BrowseShell.cs`) | Uses **`BrowserTreeRefocusAfterWizardContext`**: **`PreferredNextFolderFullPath`** (sibling folder after directory removal), **`ImageDeletionWorkingFolder`** plus **`DeletedImagePathsForRefocus`** / **`ImagePanePathsBeforeDeletion`** for in-folder image deletes (wizard + tree delete via **`RefreshBrowserPaneAfterWizardImageDeletesAsync`** merge). Awaits **`WaitForReloadAppliedAsync`** before reading **`Items`**; uses **`PickNextDisplayedPathAfterRemovalsInOrderedList`** when before/after metadata is present (**FR-BR-01**, **FR-BR-04**). |
| Deferred resort flush (wizard) | `PrepareBrowserTreeViewportAfterWizardMutation` (`MainWindow.BrowserPane.cs`) | When folder list sort is **aggregate size** or **image file count**, `RequestCoalescedFolderResortForTouchedFolderPaths` debounces sibling resorts. Wizard `Refresh*` stops that timer and applies any **pending** coalesced sibling sorts **after** commit and **before** **`ScheduleViewportAsync`** so the tree order matches sort keys when pinning. No-op for name / date sorts (no deferred pending set). |
| Viewport | **`ScheduleViewport`** (fire-and-forget) and **`ScheduleViewportAsync`** (awaitable), **`BrowserTreeViewportPump`** (`MainWindow.BrowserViewport.cs`) | All tree scrolling after browse mutations, Find, wizard refresh, sibling nav, image-step nav, keyboard tree moves, root populate, and folder-navigation flushes goes through **`BrowserTreeViewportIntent`** built by **`BrowserTreeViewportIntentResolver`** (`ImageHoard.Core.Browse`, unit-tested). See **Browser tree viewport (intent + pump)** below. |
| Cold-boot anchor (`paths.lastActedFsObject`) | **`SetLastActedFsObject`**, **`CaptureLastActedFsObjectFromCurrentTreeSelectionAfterWizardCommit`** (`MainWindow.BrowserPane.cs`), **`OnFolderTreeImageRowSelectedAsync`** (`MainWindow.PreviewNavigation.cs`), **`BrowserFindApplyMatchAsync`**, **`SwitchToBrowseAtCurrentSlideshowLocationAsync`** (`MainWindow.P0.cs`), **`FolderTree_OnExpanding`**, **`ApplyBrowserTreeLeadPreviewAsync`**, **`BrowseTreeKeyboardMoveSelection`**, **`CommitIncrementalFolderPreviewAndSelectionAsync`** (`try`/`finally` anchor capture after wizard refocus), rename paths (`RelocateAppPathsAfterFolderRename`, image rename), **`NavigateToFolderAsync`** (clears anchor when browse root changes), wizard clears browse (`MainWindow.DeleteArchiveWizard.cs`) | Persisted **`LastActedFsObject`** is the single cold-boot viewport anchor (Find, expand, tree selection, image step, sibling folder nav, keyboard tree move, wizard refocus, slideshow switch-to-browse). Slideshow-only preview commits do **not** update it. Legacy settings without `lastActedFsObject` seed from **`lastSelectedImage`** on load (`AppSettingsStore.MapToSession`). |

Long-running wizard work **outside** `Refresh*` (e.g. bulk image recycle/permanent deletes inside **`WizardExecuteImageRecycleOrPermanentBatchAsync`**, directory merge/move, recycle of whole folder) also runs under **`EnterBrowserPaneMutation`** so navigation stays blocked until refresh (or **`ReconcileBrowserPaneAfterWizardNavigateToParentAsync`**) and **awaited** viewport scheduling complete. Bulk recycle uses a single shell **`IFileOperation`** when possible; permanent deletes after explicit approval may run with limited parallelism (`File.Delete`), all on the UI STA thread except **`Task.Run`** workers for deletes.

### Browser tree viewport (intent + pump)

**Intent (Core)** — `BrowserTreeViewportIntent` is a small immutable record: **`Reason`**, **`PrimaryPath`** (folder or file row target), **`SecondaryPath`** (fallback row, e.g. parent of a file hit), **`VerticalAlignmentRatio`** (0 = pin row to top / leading; 0.5 = vertically center — cold-boot file anchor), **`PreferSelectionFirst`**, and **`Visibility`** (`BrowserTreeViewportVisibility`: default **`BringIntoView`**; **`ForImageStep`** alone sets **`PageWhenOutsideViewport`**). Reasons include cold boot, wizard delete/undo/parent-nav, folder navigation, sibling folder navigation, Find folder/file hit, image-step navigation, keyboard tree move, and root populate (`BrowserTreeViewportReason`).

**Resolver (Core)** — `BrowserTreeViewportIntentResolver` maps **`BrowserPaneState`** (+ optional wizard refocus context, find match, target paths) into an intent. Pin-after-commit rules mirror the former `GetBrowserTreeViewportPinPathAfterBrowseCommit` / browsed-directory resolution (still implemented in the host where needed; resolver exposes **`GetPinPathAfterBrowseCommit`** for tests and parity). Cold boot uses **`ForColdBootAnchor(state, lastActedFsObject)`** (null/invalid/outside-root → scroll list top; folder → **`PrimaryPath`** + ratio 0; existing file → file **`PrimaryPath`**, parent **`SecondaryPath`**, ratio 0.5, **`PreferSelectionFirst: true`**, **`Visibility: BringIntoView`** — unchanged center/top pinning vs session restore).

**Pump (App)** — `BrowserTreeViewportPump` (nested in `MainWindow.BrowserViewport.cs`) owns timing:

- **Coalescing** — Multiple **`ScheduleViewportAsync`** calls before the pump drain runs collapse to the **latest** intent; every waiter **`Task` completes** when that drain batch finishes. Intents with **`Reason == ColdBootRestore`** are **not** coalesced in the normal drain: **`ScheduleViewportAsync`** forwards them immediately to **`RunColdBootViewportAsync`** (restore code also invokes **`RunColdBootViewportAsync`** directly after selection sync).
- **Suppression** — While **`SuppressViewportForColdBoot(true)`** / **`_suppressBrowserTreeViewportMutationForColdBoot`** is set, **`ScheduleViewportAsync`** / **`ScheduleViewport`** return immediately without work (incidental viewport during restore is dropped). **Browse2** (`MainWindow.BrowseShell.cs`, **`Browse2ApplyViewportIntentAsync`**) exempts **`Reason == ColdBootRestore`** so **`RunColdBootViewportAsync`** still runs **`ScrollFolderIntoView`** while suppression is on (otherwise the folder pane stays at the origin after process restart).
- **Scroll pass** — Each run calls **`PrepareBrowserTreeViewportAfterWizardMutation`** first (flush deferred aggregate/count sibling resorts so order is final). Then **`TryScrollPassOnce`**: **`FolderTree.UpdateLayout()`**. When **`intent.Visibility == PageWhenOutsideViewport`** (image-step only), **`TryScrollPassPageWhenOutsideViewportOnce`** resolves the image **`TreeViewNode`** from **`PrimaryPath`**, measures the realized **`TreeViewItem`** against the inner folder-tree **`ScrollViewer`**, and applies **`BrowserTreeViewportPageScrollPlan.Compute`** → **`ScrollViewer.ChangeView`** in **viewport-height** steps when the row is not fully visible; it does **not** call **`TryBringFolderTreeNodeIntoView`** (so stepping does not continually align the row to the leading edge). Otherwise: resolve nodes from **`PrimaryPath`** / **`SecondaryPath`** (folders via path index; files via image-row lookup), **`PreferSelectionFirst`** vs pin-first ordering, and **`TryBringFolderTreeNodeIntoView(node, intent.VerticalAlignmentRatio)`** (inner **`TreeViewList.ScrollIntoView`** uses **`Leading`** when ratio ≤ 0, else **`Default`**; **`TreeViewItem.StartBringIntoView`** passes **`VerticalAlignmentRatio`**). Cold boot with no primary/secondary paths calls **`TryScrollFolderTreeToTop`**. If the target is not realized, the pump waits on **`FolderTree.LayoutUpdated`** (capped by **`ViewportPumpMaxWaitMs`** (250 ms) and **`ViewportPumpMaxLayoutCycles`** (8)), then retries. **Dispatcher priority**: drain starts at **Normal** when either path field is set (pinned-ish intents), else **Low**.
- **Exhaustion** — After the wait loop, a final **Normal**-priority dispatcher callback runs a best-effort fallback. For **`PageWhenOutsideViewport`**, it retries only the page-scroll path (no alignment fallback to avoid re-pinning the selection to the top). Otherwise the legacy fallback applies (including **`TryScrollFolderTreeToTop`** only when there is no valid browse directory **and** the intent is not “pinned” to an existing folder primary path — avoids snapping away from a meaningful browse pin on timeout).

**Cold boot inside the pump** — **`RunColdBootViewportAsync`**: for **`ColdBootRestore`**, flush resorts via **`PrepareBrowserTreeViewportAfterWizardMutation`**, **`UpdateLayout`**, wait one layout slice, then **`ExecuteScrollPassWithLayoutWaitAsync`** once (no persisted **`ScrollViewer`** offset restore; anchor alignment only).

### Cold boot and tree scroll persistence

1. **`RestoreStartupBrowseAsync`** sets **`SuppressViewportForColdBoot(true)`** before **`NavigateToFolderAsync(..., suppressViewportAfterRootPopulate: true, coldBootSessionRestore: true)`** and **`RestoreColdBootBrowseAfterNavigateAsync`**, then clears suppression in **`finally`**. While the flag is set, **`ScheduleViewport` / `ScheduleViewportAsync`** return without running incidental viewport work during restore. **Browse2:** **`ColdBootRestore`** intents are **not** suppressed so **`await RunColdBootViewportAsync(ForColdBootAnchor(...))`** inside **`RestoreColdBootBrowseAfterNavigateAsync`** can scroll the virtualized folder list.
2. **`coldBootSessionRestore`** defers root image selection (`deferRootSelectionForColdBootRestore`) so **`LastSelectedImage`** is not overwritten by **`FirstImageNodePreorder`** before subtrees exist.
3. **`RestoreColdBootBrowseAfterNavigateAsync`** — If the persisted **`BrowserTreeSessionSnapshot`** does not match the current browse root, resets the tree **`ScrollViewer`** to origin; otherwise expands persisted **`ExpandedFolderPaths`** shallow-first via **`TryEnsureFolderPathMaterializedAsync(..., expandAndPopulateDestinationFolder: true)`**. Then, when **`LastActedFsObject`** is set and under the browse root: **folder** anchors expand their ancestor chain plus the folder, **`SyncBrowseTreeSelection`** to the folder row, **`ApplyBrowserTreeLeadPreviewAsync`**; **file** anchors expand parent folders, **`TrySyncBrowseTreeSelectionToImagePathAsync`**, **`ApplyBrowserTreeLeadPreviewAsync`**. Finally **`await RunColdBootViewportAsync(BrowserTreeViewportIntentResolver.ForColdBootAnchor(BuildBrowserPaneState(), LastActedFsObject))`** — folder rows pin to the **top** of the viewport (`VerticalAlignmentRatio` 0); file rows use **center** alignment (ratio 0.5). Missing/stale/outside-root anchors yield a **top-of-list** scroll (empty primary/secondary in the intent). Cold-boot intents use default **`BrowserTreeViewportVisibility.BringIntoView`** (not **`PageWhenOutsideViewport`**).
4. **`FolderTreeScrollViewer_ViewChanged`** (`MainWindow.BrowserPane.cs`) skips **`SchedulePersistLayoutDebounced`** while **`_suppressBrowserTreeViewportMutationForColdBoot`** is true (non-intermediate events only), so the first restored **`ChangeView`** is not immediately overwritten in session state by layout churn.
5. **`CaptureBrowserTreeSnapshotIntoSessionIfBrowsing`** persists **`ExpandedFolderPaths`** only (no tree **`ScrollViewer`** offsets). **`paths.lastActedFsObject`** is updated by **`SetLastActedFsObject`** on user actions (see maintenance table row **Cold-boot anchor**).

## Races addressed

1. **Overlay dismissed while work continues** — Escape/Close could hide the wizard while `async` work still ran; `IsDeleteArchiveWizardOverlayOpen` no longer blocked `nav.*`. Mutation depth blocks browse commands and async browse entry points until teardown finishes.
2. **Overlapping `BrowseNavigateByStepAsync` instances** — Still possible when depth is zero; with depth, new navigations are suppressed during mutation.
3. **Viewport vs navigation** — `RefreshBrowserPaneAfterWizard*` awaits **`ScheduleViewportAsync(...)`** (viewport pump) before clearing nested mutation depth so the tree scroll pass finishes (within the implemented completion contract) before navigation unblocks. **`ReconcileBrowserPaneAfterWizardNavigateToParentAsync`** uses the same awaited **`ScheduleViewportAsync(ForWizardCommit(...))`** after a single populate + commit when the browsed folder was removed.
4. **Viewport vs deferred folder resort** — Without a pre-viewport flush, the awaited scroll could run while sibling folder rows were still in pre-resort order, then a timer-driven **`FlushCoalescedFolderResorts`** would reorder and rely on a second **`ScheduleViewport`**. Wizard `Refresh*` still calls **`PrepareBrowserTreeViewportAfterWizardMutation`** immediately before **`ScheduleViewportAsync`**; the pump repeats that flush at the start of each scroll pass so ordering stays aligned with the awaited pass and with later drains.
5. **Staged browse populate vs wizard tree refresh** — `AppendBrowserStagedToTargetAsync` used to append all image `TreeViewNode`s before the first `_populateBrowserGeneration` check, then insert folder chunks across `await Task.Yield()` boundaries while wizard `Refresh*` could reconcile the same sibling list under the same generation token. **`RefreshBrowserPaneAfterWizard*`** now increments **`_populateBrowserGeneration`** at entry, and staged append validates the token **before** appending image rows so superseded work cannot leave partial lists.
6. **Cold boot vs deferred folder resort** — **`FlushCoalescedFolderResorts`** ends with **`ScheduleViewport(ForFolderNavigation(...))`**. While **`_suppressBrowserTreeViewportMutationForColdBoot`** is true, that **`ScheduleViewport`** call is skipped so cold-boot restore is not disturbed. **`SuppressViewportForColdBoot`** still keeps non-**`ColdBootRestore`** **`ScheduleViewport` / `ScheduleViewportAsync`** as no-ops for the whole **`RestoreStartupBrowseAsync`** try block (Browse2: see **`Browse2ApplyViewportIntentAsync`**). **`FolderTreeScrollViewer_ViewChanged`** skips **`SchedulePersistLayoutDebounced`** while suppression is active.

## Cold boot manual QA (browser tree)

After changing cold-boot restore, verify on a full app restart (process exit, not just navigate away):

1. **Deep folder expand** — Expand a nested subfolder (no image preview required), exit, restart: that folder row is **at the top** of the tree viewport and **expanded**.
2. **Find folder hit** — Find-in-tree selects a deep folder match, exit, restart: same (**top**, expanded chain).
3. **Find file hit** — Find selects an image row, restart: ancestors expanded, **image row vertically centered** in the tree viewport.
4. **Image step / last image** — **`LastActedFsObject`** / legacy **`lastSelectedImage`** seed: after stepping images, restart: current file row **centered**; preview matches.
5. **No anchor** — Clear or move off-disk anchor, or first launch with no `lastActedFsObject`: tree list scrolls to **top**.
6. **Different browse root** — Open another browse folder (anchor cleared), restart: tree at **top** under the new root until a new tree action.
7. **Aggregate folder sort** — Folder list sort **Total size** or **Image file count** so metrics resort runs; cold boot still lands on the anchored row (no spurious **`FlushCoalescedFolderResorts`** viewport during suppression).

## Related design docs

- [`docs/design-decisions/slideshow-algorithm-p0.md`](../design-decisions/slideshow-algorithm-p0.md) — tree slideshow discovery, uniform sampling over full discovered set, RAM + spill path store, shuffle.
- [`docs/design-decisions/browser-folder-tree-path-to-node-index.md`](../design-decisions/browser-folder-tree-path-to-node-index.md) — **legacy** path ↔ `TreeViewNode` indexing (superseded for Browser V2).
- [`docs/design-decisions/browser-folder-tree-virtualization-itemsrepeater.md`](../design-decisions/browser-folder-tree-virtualization-itemsrepeater.md) — virtualization direction (`ItemsRepeater`).
- [`docs/design-decisions/browser-tree-rewrite-architecture.md`](../design-decisions/browser-tree-rewrite-architecture.md) — Browser V2 three-layer model and `FsDiffStream`.
- [`docs/design-decisions/browser-tree-viewport-anchor-persistence.md`](../design-decisions/browser-tree-viewport-anchor-persistence.md) — persisted viewport anchor vs `lastActedFsObject`.
